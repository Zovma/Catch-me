let t = 60;
let i = 0;

window.onload = function () {
    let butn1 = document.getElementById('butn1')
    let butn2 = document.getElementById('butn2')
    let butn3 = document.getElementById('butn3')
    let butn4 = document.getElementById('butn4')
    let butn5 = document.getElementById('butn5')
    let comment = document.getElementById('comment')
}
comment.addEventListener('mousemove', function (e) {
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)

butn1.addEventListener('mousemove', function (e) {
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn2.addEventListener('mousemove', function (e) {
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn3.addEventListener('mousemove', function (e) {
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn4.addEventListener('mousemove', function (e) {
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn5.addEventListener('mousemove', function (e) {
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn6.addEventListener('mousemove', function (e) {
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn7.addEventListener('mousemove', function (e) {
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn8.addEventListener('mousemove', function (e) {
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn9.addEventListener('mousemove', function (e) {
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn10.addEventListener('mousemove', function (e) {
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)

butn1.addEventListener('mousedown', function (e) {
    soundClick()
    chengeNum()
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn3.addEventListener('mousedown', function (e) {
    soundClick()
    chengeNum()
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn4.addEventListener('mousedown', function (e) {
    soundClick()
    chengeNum()
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn5.addEventListener('mousedown', function (e) {
    soundClick()
    chengeNum()
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn6.addEventListener('mousedown', function (e) {
    soundClick()
    chengeNum()
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn7.addEventListener('mousedown', function (e) {
    soundClick()
    chengeNum()
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn8.addEventListener('mousedown', function (e) {
    soundClick()
    chengeNum()
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn9.addEventListener('mousedown', function (e) {
    soundClick()
    chengeNum()
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)
butn10.addEventListener('mousedown', function (e) {
    soundClick()
    chengeNum()
    let num1 = getRandomIntInclusive(-20, 20)
    let num2 = getRandomIntInclusive(-20, 20)
    let position = this.getBoundingClientRect()
    let left = position.x + num1
    let top = position.y + num2
    this.style.left = left + "px"
    this.style.top = top + "px"
}, false)



function chengeNum() {
    i++
    document.getElementById('num').innerHTML = i
    if (i == 5) {
        document.getElementById('comment').innerHTML = 'Что-то в тебе есть...'
    }
    else if (i == 10) {
        document.getElementById('comment').innerHTML = 'Жми, жми! на кнопки...'
    }
    else if (i == 15) {
        document.getElementById('comment').innerHTML = 'Вау, идём на рекорд!'
    }
    else if (i == 20) {
        document.getElementById('comment').innerHTML = 'Меткость твоё второе имя!'
    }
}

function getRandomIntInclusive(min, max) {
    min = Math.ceil(min);
    max = Math.floor(max);
    return Math.floor(Math.random() * (max - min + 1)) + min; //Максимум и минимум включаются
}
function soundClick() {
    var audio = new Audio(); // Создаём новый элемент Audio
    audio.src = './Sound_11084.wav'; // Указываем путь к звуку "клика"
    audio.autoplay = true; // Автоматически запускаем
}

setInterval(function () {
    if (t == 0) {
        alert('Пойманых кнопок ' + [i] + ', Cпасибо за игру!')
        document.write("<h1>Это была великолепная игра! " + '<br>' + " Пойманых кнопок " + [i] + "</h1>")
    }
    t -= 1
    document.getElementById('timer').innerHTML = t
}, 1000)